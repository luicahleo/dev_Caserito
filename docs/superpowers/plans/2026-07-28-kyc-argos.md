# KYC automático con ARGOS — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Automatizar la decisión de KYC en CaseritoApp comparando la foto del DNI y la selfie mediante el servicio interno ARGOS, aprobando o rechazando la solicitud sin intervención humana.

**Architecture:** Se mantiene Clean Architecture/CQRS-lite: el dominio solo agrega un campo de auditoría (`ScoreSimilitud`) y errores; Application define el puerto `IVerificadorIdentidadArgos`; Infrastructure implementa el adaptador HTTP que transforma bytes a base64 y llama a `POST {ARGOS_URL}/api/verify`; Host mapea el nuevo error de disponibilidad a HTTP 503; el frontend refleja que la verificación es automática. ARGOS nunca es accesible desde el navegador.

**Tech Stack:** .NET 10, EF Core, MediatR, FluentValidation, xUnit, NSubstitute, ASP.NET Core minimal APIs, React + TypeScript + MUI + TanStack Query, openapi-typescript.

## Global Constraints

- Clean Architecture: Domain no depende hacia afuera; Application solo de Domain; Infrastructure implementa puertos; Host compone y autoriza.
- CQRS-lite: MediatR + Result + FluentValidation; reglas de negocio en dominio.
- Anti-PII no negociable: nunca registrar documentos, imágenes, embeddings, tokens, pagos, credenciales, texto de reportes ni otros datos sensibles. Usar errores genéricos.
- ARGOS solo accesible desde el backend; nunca llamarlo desde el frontend.
- Las imágenes se almacenan cifradas fuera de la BD (`IAlmacenBlobsKyc`).
- Fail-fast: fuera de `Development`/`Testing`, Caserito no debe arrancar sin `Argos:Url`.
- No hacer push/merge sin autorización explícita.
- Textos de UI y comentarios en español, con acentos y UTF-8.
- Versiones de paquetes exclusivamente en `Directory.Packages.props`.
- Preservar cambios ajenos y evitar operaciones destructivas.

---

## File Structure

| File | Responsibility |
|------|----------------|
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Domain/Kyc/SolicitudKyc.cs` | Agrega `ScoreSimilitud` nullable para auditoría del score de ARGOS. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Domain/Kyc/ErroresKyc.cs` | Nuevos códigos de error de dominio para verificación facial fallida y servicio no disponible. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Domain/Kyc/SistemaActor.cs` | Constante `Id` del actor de sistema para aprobaciones/rechazos automáticos. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/IVerificadorIdentidadArgos.cs` | Puerto y DTO de resultado de la verificación facial. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/EnviarSolicitudKycCommand.cs` | Handler actualizado: guarda blobs, llama a ARGOS, aplica decisión automática y publica `UserVerified`. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/OpcionesArgos.cs` | POCO de configuración `Argos:Url` y `Argos:ApiKey`. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/VerificadorIdentidadArgosHttp.cs` | Adaptador HTTP hacia ARGOS; convierte bytes a base64, deserializa respuesta, mapea errores de red. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/ConfiguracionKyc.cs` | Mapea `SolicitudKyc.ScoreSimilitud`. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/DependencyInjection.cs` | Registra opciones, `HttpClient` y fail-fast para `Argos:Url`. |
| `CaseritoApp/src/Host/CaseritoApp.Host/Endpoints/KycEndpoints.cs` | Maneja el nuevo error de dominio `ServicioVerificacionNoDisponible` → HTTP 503. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/DtosKyc.cs` | Extiende `SolicitudKycResumenDto` con `ScoreSimilitud` y `ResueltaPor`. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/IRepositorioVerificacionKyc.cs` | (ya existe) Se usa sin cambios de firma. |
| `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Migrations/...KycArgos.cs` | Migración EF Core para `ScoreSimilitud`. |
| `web/src/routes/KycPage.tsx` | Textos actualizados: "verificación automática". |
| `web/src/routes/AdminKycPage.tsx` | Muestra score de similitud y decisión automática; se quitan botones Aprobar/Rechazar para solicitudes pendientes. |
| `CaseritoApp/tests/CaseritoApp.UnitTests/Kyc/EnviarSolicitudKycCommandHandlerTests.cs` | Tests del handler con mock del verificador. |
| `CaseritoApp/tests/CaseritoApp.UnitTests/Kyc/VerificadorIdentidadArgosHttpTests.cs` | Tests del adaptador HTTP con `HttpMessageHandler` fake. |
| `CaseritoApp/tests/CaseritoApp.IntegrationTests/KycArgosFlujoTests.cs` | Tests de integración con Testcontainers + fake de `IVerificadorIdentidadArgos`. |
| `CaseritoApp/tests/CaseritoApp.IntegrationTests/KycFlujoTests.cs` | Se adaptan asserts al flujo automático. |

---

### Task 1: Domain — score de similitud, errores y actor de sistema

**Files:**
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Domain/Kyc/SolicitudKyc.cs`
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Domain/Kyc/ErroresKyc.cs`
- Create: `CaseritoApp/src/Identity/CaseritoApp.Identity.Domain/Kyc/SistemaActor.cs`

**Interfaces:**
- Consumes: nothing new.
- Produces: `SolicitudKyc.ScoreSimilitud`, `ErroresKyc.VerificacionFacialFallida`, `ErroresKyc.ServicioVerificacionNoDisponible`, `SistemaActor.Id`.

- [ ] **Step 1: Agregar `ScoreSimilitud` a `SolicitudKyc`**

```csharp
public double? ScoreSimilitud { get; private set; }
```

Agregar método interno para asignar el score:

```csharp
internal void RegistrarScoreSimilitud(double score)
{
    ScoreSimilitud = score;
}
```

- [ ] **Step 2: Extender `ErroresKyc` con códigos nuevos**

```csharp
public const string VerificacionFacialFallida = "Kyc.VerificacionFacialFallida";
public const string ServicioVerificacionNoDisponible = "Kyc.ServicioVerificacionNoDisponible";
```

- [ ] **Step 3: Crear `SistemaActor.cs`**

```csharp
namespace CaseritoApp.Identity.Domain.Kyc;

/// <summary>Actor de sistema para resoluciones automáticas (p. ej. integración con ARGOS).</summary>
public static class SistemaActor
{
    public static readonly Guid Id = new("00000000-0000-0000-0000-000000000001");
}
```

- [ ] **Step 4: Verificar build del dominio**

Run (desde `CaseritoApp/`):

```powershell
dotnet build src/Identity/CaseritoApp.Identity.Domain/CaseritoApp.Identity.Domain.csproj
```

Expected: build success, 0 warnings.

- [ ] **Step 5: Commit**

```bash
git add src/Identity/CaseritoApp.Identity.Domain/Kyc/SolicitudKyc.cs

git add src/Identity/CaseritoApp.Identity.Domain/Kyc/ErroresKyc.cs

git add src/Identity/CaseritoApp.Identity.Domain/Kyc/SistemaActor.cs

git commit -m "feat(kyc-argos): score, errores y actor de sistema en dominio"
```

---

### Task 2: Application — puerto del verificador ARGOS

**Files:**
- Create: `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/IVerificadorIdentidadArgos.cs`

**Interfaces:**
- Consumes: nothing new.
- Produces: `IVerificadorIdentidadArgos.VerificarAsync(byte[] imagenDocumento, byte[] imagenSelfie, CancellationToken ct)` → `Task<Result<VerificacionFacialResultado>>`.

- [ ] **Step 1: Definir el puerto y el DTO de resultado**

```csharp
using CaseritoApp.BuildingBlocks.Domain;

namespace CaseritoApp.Identity.Application.Kyc;

/// <summary>Puerto hacia un servicio de comparación facial 1:1 (ARGOS).</summary>
public interface IVerificadorIdentidadArgos
{
    /// <summary>Compara documento y selfie. No loguea bytes ni base64.</summary>
    Task<Result<VerificacionFacialResultado>> VerificarAsync(
        byte[] imagenDocumento, byte[] imagenSelfie, CancellationToken ct);
}

/// <summary>Resultado de la comparación facial.</summary>
/// <param name="Coinciden">true si el rostro coincide con el documento.</param>
/// <param name="SimilitudPercent">Score de similitud devuelto por ARGOS (0-100).</param>
/// <param name="MotivoRechazo">Motivo legible cuando <see cref="Coinciden"/> es false.</param>
public sealed record VerificacionFacialResultado(
    bool Coinciden,
    double SimilitudPercent,
    string? MotivoRechazo);
```

- [ ] **Step 2: Verificar build de Application**

Run:

```powershell
dotnet build src/Identity/CaseritoApp.Identity.Application/CaseritoApp.Identity.Application.csproj
```

Expected: build success.

- [ ] **Step 3: Commit**

```bash
git add src/Identity/CaseritoApp.Identity.Application/Kyc/IVerificadorIdentidadArgos.cs

git commit -m "feat(kyc-argos): puerto IVerificadorIdentidadArgos en Application"
```

---

### Task 3: Application — actualizar `EnviarSolicitudKycCommandHandler`

**Files:**
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/EnviarSolicitudKycCommand.cs`

**Interfaces:**
- Consumes: `IVerificadorIdentidadArgos`, `VerificacionFacialResultado`, `SistemaActor.Id`, `IPublicadorEventosIntegracion` (ya usado en `AprobarSolicitudKycCommandHandler`).
- Produces: handler que decide automáticamente `Aprobada`/`Rechazada` y publica `UserVerified` en caso de éxito.

- [ ] **Step 1: Inyectar `IVerificadorIdentidadArgos` y `IPublicadorEventosIntegracion`**

```csharp
public sealed partial class EnviarSolicitudKycCommandHandler(
    IRepositorioVerificacionKyc repositorio,
    IAlmacenBlobsKyc almacen,
    IVerificadorIdentidadArgos verificador,
    IPublicadorEventosIntegracion publicador,
    TimeProvider tiempo,
    ILogger<EnviarSolicitudKycCommandHandler> logger)
    : ICommandHandler<EnviarSolicitudKycCommand>
```

- [ ] **Step 2: Implementar flujo de envío automático**

Estructura final del handler:

```csharp
public async Task<Result> Handle(EnviarSolicitudKycCommand request, CancellationToken cancellationToken)
{
    var verificacion = await repositorio.ObtenerPorUsuarioAsync(request.UsuarioId, cancellationToken);
    var esNueva = verificacion is null;
    verificacion ??= VerificacionKyc.Crear(request.UsuarioId);

    // Validar invariants antes de tocar blobs o llamar a ARGOS.
    var validacionPrevia = verificacion.PuedeEnviarSolicitud();
    if (!validacionPrevia.EsExito)
    {
        RegistrarEnvio(logger, request.UsuarioId, validacionPrevia.Error.Code);
        return Result.Fallo(validacionPrevia.Error);
    }

    var claveDoc = await almacen.GuardarAsync(request.Documento, request.DocumentoContentType, cancellationToken);
    var claveSelfie = await almacen.GuardarAsync(request.Selfie, request.SelfieContentType, cancellationToken);

    var resultadoArgos = await verificador.VerificarAsync(request.Documento, request.Selfie, cancellationToken);

    if (!resultadoArgos.EsExito)
    {
        // Compensación: ARGOS no responde, eliminar blobs y no persistir nada.
        await almacen.EliminarAsync(claveDoc, cancellationToken);
        await almacen.EliminarAsync(claveSelfie, cancellationToken);
        RegistrarEnvio(logger, request.UsuarioId, resultadoArgos.Error.Code);
        return Result.Fallo(resultadoArgos.Error);
    }

    var facial = resultadoArgos.Valor;

    var resultado = verificacion.EnviarSolicitud(
        claveDoc, claveSelfie, TipoDocumento.CedulaIdentidad, tiempo.GetUtcNow());

    if (!resultado.EsExito)
    {
        await almacen.EliminarAsync(claveDoc, cancellationToken);
        await almacen.EliminarAsync(claveSelfie, cancellationToken);
        RegistrarEnvio(logger, request.UsuarioId, resultado.Error.Code);
        return Result.Fallo(resultado.Error);
    }

    var solicitud = resultado.Valor;
    solicitud.RegistrarScoreSimilitud(facial.SimilitudPercent);

    var ahora = tiempo.GetUtcNow();
    var resolucion = facial.Coinciden
        ? verificacion.Aprobar(solicitud.Id, SistemaActor.Id, ahora)
        : verificacion.Rechazar(solicitud.Id, SistemaActor.Id, facial.MotivoRechazo ?? "El rostro no coincide con el documento.", ahora);

    if (!resolucion.EsExito)
    {
        // Estado inconsistente: esto no debería ocurrir porque acabamos de crear la solicitud.
        await almacen.EliminarAsync(claveDoc, cancellationToken);
        await almacen.EliminarAsync(claveSelfie, cancellationToken);
        RegistrarEnvio(logger, request.UsuarioId, resolucion.Error.Code);
        return Result.Fallo(resolucion.Error);
    }

    if (facial.Coinciden)
    {
        await publicador.PublicarAsync(
            new UserVerified(Guid.NewGuid(), ahora, verificacion.UsuarioId), cancellationToken);
    }

    if (esNueva)
    {
        repositorio.Agregar(verificacion);
    }

    RegistrarEnvio(logger, request.UsuarioId, facial.Coinciden ? "aprobado-automatico" : "rechazado-automatico");
    return Result.Exito();
}
```

> Nota: el plan asume agregar `PuedeEnviarSolicitud()` a `VerificacionKyc` para evitar crear blobs si hay una solicitud pendiente o ya verificado. Si se prefiere no tocar `VerificacionKyc`, ejecutar `EnviarSolicitud` con datos dummy y desechar el resultado, pero eso es menos limpio. Añadir `PuedeEnviarSolicitud` en Task 1 como Step 1b.

- [ ] **Step 3: Añadir `PuedeEnviarSolicitud()` en `VerificacionKyc.cs` (alternativa recomendada)**

```csharp
public Result PuedeEnviarSolicitud()
{
    if (EstaVerificado)
    {
        return Result.Fallo(new Error(ErroresKyc.YaVerificado, "El usuario ya está verificado."));
    }

    if (SolicitudActual?.Estado == EstadoKyc.Pendiente)
    {
        return Result.Fallo(new Error(ErroresKyc.SolicitudPendienteExiste, "Ya existe una solicitud pendiente."));
    }

    return Result.Exito();
}
```

- [ ] **Step 4: Verificar build de Application**

Run:

```powershell
dotnet build CaseritoApp.sln
```

Expected: build success ( Domain + Application compilan; Infrastructure/Host pueden fallar por el puerto no registrado aún).

- [ ] **Step 5: Commit**

```bash
git add src/Identity/CaseritoApp.Identity.Application/Kyc/EnviarSolicitudKycCommand.cs

git add src/Identity/CaseritoApp.Identity.Domain/Kyc/VerificacionKyc.cs

git commit -m "feat(kyc-argos): envio automático aprueba/rechaza segun ARGOS"
```

---

### Task 4: Infrastructure — adaptador HTTP a ARGOS

**Files:**
- Create: `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/OpcionesArgos.cs`
- Create: `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/VerificadorIdentidadArgosHttp.cs`
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/DependencyInjection.cs`

**Interfaces:**
- Consumes: `IVerificadorIdentidadArgos`, `VerificacionFacialResultado`, `OpcionesArgos`.
- Produces: adaptador registrado con `AddHttpClient`.

- [ ] **Step 1: Crear `OpcionesArgos.cs`**

```csharp
namespace CaseritoApp.Identity.Infrastructure.Kyc;

/// <summary>Configuración de conexión al servicio ARGOS.</summary>
public sealed class OpcionesArgos
{
    public const string Seccion = "Argos";

    /// <summary>URL base de ARGOS, sin barra final.</summary>
    public string Url { get; set; } = string.Empty;

    /// <summary>API key para el header X-Service-Key. Opcional.</summary>
    public string? ApiKey { get; set; }
}
```

- [ ] **Step 2: Crear `VerificadorIdentidadArgosHttp.cs`**

```csharp
using System.Net.Http.Json;
using System.Text.Json.Serialization;
using CaseritoApp.BuildingBlocks.Domain;
using CaseritoApp.Identity.Application.Kyc;
using Microsoft.Extensions.Logging;
using Microsoft.Extensions.Options;

namespace CaseritoApp.Identity.Infrastructure.Kyc;

/// <summary>Adaptador HTTP hacia ARGOS. No loguea imágenes, base64 ni embeddings.</summary>
public sealed partial class VerificadorIdentidadArgosHttp(
    HttpClient httpClient,
    IOptions<OpcionesArgos> opciones,
    ILogger<VerificadorIdentidadArgosHttp> logger)
    : IVerificadorIdentidadArgos
{
    public async Task<Result<VerificacionFacialResultado>> VerificarAsync(
        byte[] imagenDocumento, byte[] imagenSelfie, CancellationToken ct)
    {
        var url = opciones.Value.Url.TrimEnd('/') + "/api/verify";
        var request = new VerificarRequest(
            Convert.ToBase64String(imagenDocumento),
            Convert.ToBase64String(imagenSelfie));

        using var mensaje = new HttpRequestMessage(HttpMethod.Post, url)
        {
            Content = JsonContent.Create(request)
        };

        if (!string.IsNullOrWhiteSpace(opciones.Value.ApiKey))
        {
            mensaje.Headers.TryAddWithoutValidation("X-Service-Key", opciones.Value.ApiKey);
        }

        HttpResponseMessage respuesta;
        try
        {
            respuesta = await httpClient.SendAsync(mensaje, ct);
        }
        catch (OperationCanceledException ex) when (ex.CancellationToken == ct)
        {
            RegistrarError(logger, "timeout");
            return Result.Fallo<VerificacionFacialResultado>(
                new Error(ErroresKyc.ServicioVerificacionNoDisponible,
                    "El servicio de verificación no está disponible. Intente más tarde."));
        }
        catch (HttpRequestException)
        {
            RegistrarError(logger, "red");
            return Result.Fallo<VerificacionFacialResultado>(
                new Error(ErroresKyc.ServicioVerificacionNoDisponible,
                    "El servicio de verificación no está disponible. Intente más tarde."));
        }

        if (!respuesta.IsSuccessStatusCode)
        {
            RegistrarError(logger, $"http-{(int)respuesta.StatusCode}");
            return Result.Fallo<VerificacionFacialResultado>(
                new Error(ErroresKyc.ServicioVerificacionNoDisponible,
                    "El servicio de verificación no está disponible. Intente más tarde."));
        }

        VerificarResponse? cuerpo;
        try
        {
            cuerpo = await respuesta.Content.ReadFromJsonAsync<VerificarResponse>(ct);
        }
        catch (Exception ex)
        {
            RegistrarError(logger, $"json-{ex.GetType().Name}");
            return Result.Fallo<VerificacionFacialResultado>(
                new Error(ErroresKyc.ServicioVerificacionNoDisponible,
                    "El servicio de verificación no está disponible. Intente más tarde."));
        }

        if (cuerpo is null)
        {
            RegistrarError(logger, "respuesta-vacia");
            return Result.Fallo<VerificacionFacialResultado>(
                new Error(ErroresKyc.ServicioVerificacionNoDisponible,
                    "El servicio de verificación no está disponible. Intente más tarde."));
        }

        if (!cuerpo.Success)
        {
            RegistrarError(logger, "success-false");
            return Result.Fallo<VerificacionFacialResultado>(
                new Error(ErroresKyc.VerificacionFacialFallida,
                    cuerpo.Message ?? "No se pudo verificar el rostro."));
        }

        if (!cuerpo.Verified)
        {
            return Result.Exito(new VerificacionFacialResultado(
                Coinciden: false,
                SimilitudPercent: cuerpo.SimilarityPercent,
                MotivoRechazo: cuerpo.Message ?? "El rostro no coincide con el documento."));
        }

        return Result.Exito(new VerificacionFacialResultado(
            Coinciden: true,
            SimilitudPercent: cuerpo.SimilarityPercent,
            MotivoRechazo: null));
    }

    private sealed record VerificarRequest(string Image1, string Image2);

    private sealed record VerificarResponse(
        [property: JsonPropertyName("success")] bool Success,
        [property: JsonPropertyName("verified")] bool Verified,
        [property: JsonPropertyName("distance")] double? Distance,
        [property: JsonPropertyName("threshold")] double? Threshold,
        [property: JsonPropertyName("similarity_percent")] double SimilarityPercent,
        [property: JsonPropertyName("message")] string? Message);

    // Auditoría sin PII: solo tipo de error de comunicación. Sin URLs completas, base64 ni bytes.
    [LoggerMessage(Level = LogLevel.Warning, Message = "ARGOS no disponible: tipo={TipoError}")]
    private static partial void RegistrarError(ILogger logger, string tipoError);
}
```

- [ ] **Step 3: Registrar en `DependencyInjection.cs`**

Dentro de `AgregarIdentity`, antes del fail-fast de IEncryptor:

```csharp
servicios.Configure<OpcionesArgos>(config.GetSection(OpcionesArgos.Seccion));

var permiteArgosOpcional = entorno.IsDevelopment() || entorno.IsEnvironment("Testing");
servicios.AddOptions<OpcionesArgos>()
    .Validate(
        o => permiteArgosOpcional || !string.IsNullOrWhiteSpace(o.Url),
        "Argos:Url es obligatorio fuera de Development/Testing")
    .ValidateOnStart();

servicios.AddHttpClient<IVerificadorIdentidadArgos, VerificadorIdentidadArgosHttp>();
```

- [ ] **Step 4: Verificar build de Infrastructure**

Run:

```powershell
dotnet build src/Identity/CaseritoApp.Identity.Infrastructure/CaseritoApp.Identity.Infrastructure.csproj
```

Expected: build success.

- [ ] **Step 5: Commit**

```bash
git add src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/OpcionesArgos.cs

git add src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/VerificadorIdentidadArgosHttp.cs

git add src/Identity/CaseritoApp.Identity.Infrastructure/DependencyInjection.cs

git commit -m "feat(kyc-argos): adaptador HTTP a ARGOS y registro DI"
```

---

### Task 5: Infrastructure — mapeo EF Core y migración

**Files:**
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/ConfiguracionKyc.cs`
- Create: migración EF Core en `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Migrations/`

**Interfaces:**
- Consumes: `SolicitudKyc.ScoreSimilitud`.
- Produces: columna `ScoreSimilitud` nullable en `SolicitudesKyc`.

- [ ] **Step 1: Mapear `ScoreSimilitud` en `ConfiguracionKyc.cs`**

```csharp
builder.Entity<SolicitudKyc>(e =>
{
    // ... existing config ...
    e.Property(s => s.ScoreSimilitud);
});
```

- [ ] **Step 2: Generar migración**

Run (desde `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/`):

```powershell
dotnet ef migrations add KycArgosScoreSimilitud --startup-project ../../Host/CaseritoApp.Host/CaseritoApp.Host.csproj
```

Expected: se crean `..._KycArgosScoreSimilitud.cs` y `.Designer.cs`.

- [ ] **Step 3: Verificar migración**

Revisar que la migración solo agregue:

```csharp
migrationBuilder.AddColumn<double>(
    name: "ScoreSimilitud",
    table: "SolicitudesKyc",
    type: "float",
    nullable: true);
```

- [ ] **Step 4: Build**

Run:

```powershell
dotnet build CaseritoApp.sln
```

Expected: success.

- [ ] **Step 5: Commit**

```bash
git add src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/ConfiguracionKyc.cs

git add src/Identity/CaseritoApp.Identity.Infrastructure/Migrations/*KycArgosScoreSimilitud*

git commit -m "feat(kyc-argos): mapeo y migracion EF Core para ScoreSimilitud"
```

---

### Task 6: Host — mapear error de servicio no disponible a 503

**Files:**
- Modify: `CaseritoApp/src/Host/CaseritoApp.Host/Endpoints/KycEndpoints.cs`

**Interfaces:**
- Consumes: `ErroresKyc.ServicioVerificacionNoDisponible`.
- Produces: endpoint `/api/kyc` devuelve 503 cuando ARGOS no responde.

- [ ] **Step 1: Extender `DesdeResult` con el nuevo error**

```csharp
ErroresKyc.ServicioVerificacionNoDisponible =>
    Results.Problem(title: resultado.Error.Code, detail: resultado.Error.Message, statusCode: StatusCodes.Status503ServiceUnavailable),
```

Actualizar `Produces` del endpoint POST:

```csharp
.ProducesProblem(StatusCodes.Status503ServiceUnavailable)
```

- [ ] **Step 2: Verificar build de Host**

Run:

```powershell
dotnet build src/Host/CaseritoApp.Host/CaseritoApp.Host.csproj
```

Expected: success.

- [ ] **Step 3: Commit**

```bash
git add src/Host/CaseritoApp.Host/Endpoints/KycEndpoints.cs

git commit -m "feat(kyc-argos): mapea error de ARGOS a HTTP 503"
```

---

### Task 7: Application — exponer score en el listado admin

**Files:**
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Application/Kyc/DtosKyc.cs`
- Modify: `CaseritoApp/src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/RepositorioVerificacionKycEfCore.cs`

**Interfaces:**
- Consumes: `SolicitudKyc.ScoreSimilitud`, `SolicitudKyc.ResueltaPor`.
- Produces: `SolicitudKycResumenDto` con `ScoreSimilitud` y `ResueltaPor`.

- [ ] **Step 1: Extender el DTO**

```csharp
public sealed record SolicitudKycResumenDto(
    Guid SolicitudId,
    Guid UsuarioId,
    string Estado,
    string TipoDocumento,
    DateTimeOffset EnviadaEn,
    DateTimeOffset? ResueltaEn,
    double? ScoreSimilitud,
    Guid? ResueltaPor);
```

- [ ] **Step 2: Actualizar proyección en repositorio**

Buscar la proyección `Select(...)` en `RepositorioVerificacionKycEfCore.cs` y agregar:

```csharp
ScoreSimilitud = s.ScoreSimilitud,
ResueltaPor = s.ResueltaPor,
```

- [ ] **Step 3: Verificar build**

Run:

```powershell
dotnet build CaseritoApp.sln
```

Expected: success.

- [ ] **Step 4: Commit**

```bash
git add src/Identity/CaseritoApp.Identity.Application/Kyc/DtosKyc.cs

git add src/Identity/CaseritoApp.Identity.Infrastructure/Kyc/RepositorioVerificacionKycEfCore.cs

git commit -m "feat(kyc-argos): expone score y resolutor en listado admin"
```

---

### Task 8: Frontend — actualizar mensajes de KYC automático

**Files:**
- Modify: `web/src/routes/KycPage.tsx`

**Interfaces:**
- Consumes: API sin cambios de contrato.
- Produces: textos que indican verificación automática y manejo del error 503.

- [ ] **Step 1: Actualizar mensajes en `KycPage.tsx`**

Cambios mínimos:

```tsx
<Typography variant="h4" component="h1" gutterBottom>
  Verificación de identidad automática
</Typography>
```

```tsx
{estado?.estado === 'Pendiente' && (
  <Alert severity="info" sx={{ my: 2 }}>
    Tu solicitud está siendo verificada automáticamente. Te avisaremos cuando haya una decisión.
  </Alert>
)}
```

```tsx
<Typography variant="body2" color="text.secondary">
  Sube una foto de tu documento de identidad y una selfie. Nuestro sistema las comparará
  automáticamente. Formatos JPG o PNG, máximo 5 MB cada uno.
</Typography>
```

Añadir manejo del 503:

```tsx
const es503 = mutacion.error instanceof HttpError && mutacion.error.status === 503;
```

```tsx
{mutacion.isError && es503 && (
  <Alert severity="warning">
    El servicio de verificación no está disponible en este momento. Inténtalo más tarde.
  </Alert>
)}
```

- [ ] **Step 2: Verificar typecheck y lint**

Run (desde `web/`):

```powershell
npm run typecheck
npm run lint
```

Expected: 0 errores.

- [ ] **Step 3: Commit**

```bash
git add web/src/routes/KycPage.tsx

git commit -m "feat(kyc-argos): mensajes de verificacion automatica en KycPage"
```

---

### Task 9: Frontend — admin ve score y decisión automática

**Files:**
- Modify: `web/src/routes/AdminKycPage.tsx`
- Modify: `web/src/api/kyc.ts` (solo si el tipo generado no incluye los campos nuevos)

**Interfaces:**
- Consumes: `SolicitudKycResumenDto.ScoreSimilitud`, `SolicitudKycResumenDto.ResueltaPor`.
- Produces: UI de admin sin botones Aprobar/Rechazar para pendientes; muestra score.

- [ ] **Step 1: Regenerar tipos desde OpenAPI (si aplica)**

Run:

```powershell
npm run generate:api
```

Expected: `web/src/api/schema.d.ts` incluye `ScoreSimilitud` y `ResueltaPor` en `SolicitudKycResumenDto`.

- [ ] **Step 2: Actualizar columnas de la tabla**

```tsx
<TableHead>
  <TableRow>
    <TableCell>Usuario</TableCell>
    <TableCell>Documento</TableCell>
    <TableCell>Enviada</TableCell>
    <TableCell>Estado</TableCell>
    <TableCell>Score</TableCell>
    <TableCell>Resolutor</TableCell>
    <TableCell />
  </TableRow>
</TableHead>
```

```tsx
<TableCell>{s.scoreSimilitud?.toFixed(1) ?? '-'}</TableCell>
<TableCell>{s.resueltaPor === '00000000-0000-0000-0000-000000000001' ? 'Sistema' : (s.resueltaPor ?? '-')}</TableCell>
```

- [ ] **Step 3: Quitar acciones de aprobar/rechazar para pendientes**

En `PanelRevision`, cuando `solicitud.estado === 'Pendiente'` mostrar solo:

```tsx
<Typography variant="body2" color="text.secondary" sx={{ mr: 2 }}>
  Esta solicitud será resuelta automáticamente por el sistema.
</Typography>
```

Y eliminar el render condicional de botones Aprobar/Rechazar para ese estado.

- [ ] **Step 4: Actualizar tests de `AdminKycPage.test.tsx`**

Ajustar tests que esperen botones "Aprobar"/"Rechazar" para que ahora esperen el mensaje de resolución automática o el score.

- [ ] **Step 5: Verificar typecheck, lint y tests**

Run:

```powershell
npm run typecheck
npm run lint
npm run test -- --run
```

Expected: success.

- [ ] **Step 6: Commit**

```bash
git add web/src/api/schema.d.ts

git add web/src/routes/AdminKycPage.tsx

git add web/src/routes/AdminKycPage.test.tsx

git commit -m "feat(kyc-argos): admin muestra score y decision automatica"
```

---

### Task 10: Tests unitarios — handler y adaptador HTTP

**Files:**
- Modify: `CaseritoApp/tests/CaseritoApp.UnitTests/Kyc/EnviarSolicitudKycCommandHandlerTests.cs`
- Create: `CaseritoApp/tests/CaseritoApp.UnitTests/Kyc/VerificadorIdentidadArgosHttpTests.cs`

**Interfaces:**
- Consumes: handler con mocks de `IVerificadorIdentidadArgos` y `IPublicadorEventosIntegracion`.
- Produces: tests verdes.

- [ ] **Step 1: Actualizar tests del handler**

Añadir fakes/mocks:

```csharp
private sealed class VerificadorFake(Result<VerificacionFacialResultado> resultado) : IVerificadorIdentidadArgos
{
    public Task<Result<VerificacionFacialResultado>> VerificarAsync(
        byte[] imagenDocumento, byte[] imagenSelfie, CancellationToken ct) =>
        Task.FromResult(resultado);
}

private sealed class PublicadorFake : IPublicadorEventosIntegracion
{
    public List<object> Eventos { get; } = [];
    public Task PublicarAsync<T>(T evento, CancellationToken ct) where T : class
    {
        Eventos.Add(evento);
        return Task.CompletedTask;
    }
}
```

Casos de test:

```csharp
[Fact]
public async Task Envio_con_coincidencia_automatica_aprueba_y_publica_UserVerified()
{
    var usuarioId = Guid.NewGuid();
    var almacen = new AlmacenFake();
    var repo = new RepoFake(null);
    var verificador = new VerificadorFake(Result.Exito(new VerificacionFacialResultado(true, 92.5, null)));
    var publicador = new PublicadorFake();
    var handler = new EnviarSolicitudKycCommandHandler(
        repo, almacen, verificador, publicador, TimeProvider.System, NullLogger<EnviarSolicitudKycCommandHandler>.Instance);

    var r = await handler.Handle(new EnviarSolicitudKycCommand(
        usuarioId, _png, "image/png", _png, "image/png"), CancellationToken.None);

    Assert.True(r.EsExito);
    Assert.Single(publicador.Eventos.OfType<UserVerified>());
    Assert.True(repo.Agregada!.EstaVerificado);
    Assert.Equal(92.5, repo.Agregada.Solicitudes.Single().ScoreSimilitud);
}

[Fact]
public async Task Envio_sin_coincidencia_automatica_rechaza_sin_publicar_evento()
{
    var usuarioId = Guid.NewGuid();
    var almacen = new AlmacenFake();
    var repo = new RepoFake(null);
    var verificador = new VerificadorFake(Result.Exito(new VerificacionFacialResultado(false, 32.0, "El rostro no coincide")));
    var publicador = new PublicadorFake();
    var handler = new EnviarSolicitudKycCommandHandler(
        repo, almacen, verificador, publicador, TimeProvider.System, NullLogger<EnviarSolicitudKycCommandHandler>.Instance);

    var r = await handler.Handle(new EnviarSolicitudKycCommand(
        usuarioId, _png, "image/png", _png, "image/png"), CancellationToken.None);

    Assert.True(r.EsExito);
    Assert.Empty(publicador.Eventos);
    Assert.Equal(EstadoKyc.Rechazada, repo.Agregada!.Solicitudes.Single().Estado);
}

[Fact]
public async Task Envio_con_argos_caido_compensa_blobs_y_devuelve_503()
{
    var usuarioId = Guid.NewGuid();
    var almacen = new AlmacenFake();
    var repo = new RepoFake(null);
    var verificador = new VerificadorFake(Result.Fallo<VerificacionFacialResultado>(
        new Error(ErroresKyc.ServicioVerificacionNoDisponible, "No disponible")));
    var publicador = new PublicadorFake();
    var handler = new EnviarSolicitudKycCommandHandler(
        repo, almacen, verificador, publicador, TimeProvider.System, NullLogger<EnviarSolicitudKycCommandHandler>.Instance);

    var r = await handler.Handle(new EnviarSolicitudKycCommand(
        usuarioId, _png, "image/png", _png, "image/png"), CancellationToken.None);

    Assert.False(r.EsExito);
    Assert.Equal(ErroresKyc.ServicioVerificacionNoDisponible, r.Error.Code);
    Assert.Equal(2, almacen.Guardados);
    Assert.Equal(2, almacen.Eliminados);
    Assert.Null(repo.Agregada);
}
```

- [ ] **Step 2: Crear tests del adaptador HTTP**

```csharp
using System.Net;
using CaseritoApp.BuildingBlocks.Domain;
using CaseritoApp.Identity.Application.Kyc;
using CaseritoApp.Identity.Domain.Kyc;
using CaseritoApp.Identity.Infrastructure.Kyc;
using Microsoft.Extensions.Logging.Abstractions;
using Microsoft.Extensions.Options;
using Xunit;

namespace CaseritoApp.UnitTests.Kyc;

public sealed class VerificadorIdentidadArgosHttpTests
{
    private static readonly byte[] _imagen = [0x89, 0x50, 0x4E, 0x47];

    private static VerificadorIdentidadArgosHttp Crear(
        HttpMessageHandler handler, string url = "http://argos.test", string? apiKey = null)
    {
        var client = new HttpClient(handler) { BaseAddress = new Uri(url) };
        var opciones = Options.Create(new OpcionesArgos { Url = url, ApiKey = apiKey });
        return new VerificadorIdentidadArgosHttp(client, opciones, NullLogger<VerificadorIdentidadArgosHttp>.Instance);
    }

    [Fact]
    public async Task Respuesta_verified_true_devuelve_coinciden_con_score()
    {
        var handler = new FakeHandler(new HttpResponseMessage(HttpStatusCode.OK)
        {
            Content = new StringContent("""{"success": true, "verified": true, "similarity_percent": 87.5}"""),
        });
        var verificador = Crear(handler);

        var r = await verificador.VerificarAsync(_imagen, _imagen, CancellationToken.None);

        Assert.True(r.EsExito);
        Assert.True(r.Valor.Coinciden);
        Assert.Equal(87.5, r.Valor.SimilitudPercent);
    }

    [Fact]
    public async Task Respuesta_verified_false_devuelve_no_coinciden()
    {
        var handler = new FakeHandler(new HttpResponseMessage(HttpStatusCode.OK)
        {
            Content = new StringContent("""{"success": true, "verified": false, "similarity_percent": 32.0, "message": "No coincide"}"""),
        });
        var verificador = Crear(handler);

        var r = await verificador.VerificarAsync(_imagen, _imagen, CancellationToken.None);

        Assert.True(r.EsExito);
        Assert.False(r.Valor.Coinciden);
        Assert.Equal("No coincide", r.Valor.MotivoRechazo);
    }

    [Fact]
    public async Task Error_de_red_devuelve_servicio_no_disponible()
    {
        var handler = new FakeHandler(new HttpRequestException("No route"));
        var verificador = Crear(handler);

        var r = await verificador.VerificarAsync(_imagen, _imagen, CancellationToken.None);

        Assert.False(r.EsExito);
        Assert.Equal(ErroresKyc.ServicioVerificacionNoDisponible, r.Error.Code);
    }

    [Fact]
    public async Task Http_500_devuelve_servicio_no_disponible()
    {
        var handler = new FakeHandler(new HttpResponseMessage(HttpStatusCode.InternalServerError));
        var verificador = Crear(handler);

        var r = await verificador.VerificarAsync(_imagen, _imagen, CancellationToken.None);

        Assert.False(r.EsExito);
        Assert.Equal(ErroresKyc.ServicioVerificacionNoDisponible, r.Error.Code);
    }

    private sealed class FakeHandler : HttpMessageHandler
    {
        private readonly HttpResponseMessage? _respuesta;
        private readonly Exception? _excepcion;

        public FakeHandler(HttpResponseMessage respuesta) => _respuesta = respuesta;
        public FakeHandler(Exception excepcion) => _excepcion = excepcion;

        protected override Task<HttpResponseMessage> SendAsync(HttpRequestMessage request, CancellationToken cancellationToken)
        {
            if (_excepcion is not null) return Task.FromException<HttpResponseMessage>(_excepcion);
            return Task.FromResult(_respuesta!);
        }
    }
}
```

- [ ] **Step 3: Ejecutar tests unitarios**

Run:

```powershell
dotnet test tests/CaseritoApp.UnitTests/CaseritoApp.UnitTests.csproj --filter "FullyQualifiedName~Kyc"
```

Expected: all green.

- [ ] **Step 4: Commit**

```bash
git add tests/CaseritoApp.UnitTests/Kyc/EnviarSolicitudKycCommandHandlerTests.cs

git add tests/CaseritoApp.UnitTests/Kyc/VerificadorIdentidadArgosHttpTests.cs

git commit -m "test(kyc-argos): unit tests para handler y adaptador ARGOS"
```

---

### Task 11: Tests de integración — flujo completo con ARGOS mockeado

**Files:**
- Create: `CaseritoApp/tests/CaseritoApp.IntegrationTests/KycArgosFlujoTests.cs`
- Modify: `CaseritoApp/tests/CaseritoApp.IntegrationTests/KycFlujoTests.cs` (opcional, para reflejar flujo automático)

**Interfaces:**
- Consumes: `IVerificadorIdentidadArgos` reemplazado en `WebApplicationFactory`.
- Produces: tests verdes que requieren Docker.

- [ ] **Step 1: Crear `KycArgosFlujoTests.cs`**

```csharp
using System.Net;
using System.Net.Http.Headers;
using System.Net.Http.Json;
using CaseritoApp.Host.Endpoints;
using CaseritoApp.Identity.Application.Kyc;
using CaseritoApp.Identity.Domain.Autorizacion;
using CaseritoApp.Identity.Domain.Kyc;
using CaseritoApp.Identity.Infrastructure;
using CaseritoApp.IntegrationTests.Infrastructure;
using Microsoft.AspNetCore.Identity;
using Microsoft.Extensions.DependencyInjection;
using Xunit;

namespace CaseritoApp.IntegrationTests;

public sealed class KycArgosFlujoTests(CaseritoApiFactory factory) : IClassFixture<CaseritoApiFactory>
{
    private static readonly byte[] _png = [0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A, 0x00, 0x01];

    private static string Email(string prefijo) => $"{prefijo}-{Guid.NewGuid():N}@caserito.test";

    private static MultipartFormDataContent Formulario()
    {
        var contenido = new MultipartFormDataContent();
        var doc = new ByteArrayContent(_png);
        doc.Headers.ContentType = new MediaTypeHeaderValue("image/png");
        contenido.Add(doc, "documento", "ci.png");
        var selfie = new ByteArrayContent(_png);
        selfie.Headers.ContentType = new MediaTypeHeaderValue("image/png");
        contenido.Add(selfie, "selfie", "selfie.png");
        return contenido;
    }

    private static HttpRequestMessage Autorizada(HttpMethod metodo, string url, string token)
    {
        var solicitud = new HttpRequestMessage(metodo, url);
        solicitud.Headers.Authorization = new AuthenticationHeaderValue("Bearer", token);
        return solicitud;
    }

    private async Task<string> RegistrarYLoguearAsync(HttpClient cliente, string email, string? rolExtra)
    {
        var registro = await cliente.PostAsJsonAsync(
            "/api/auth/register", new RegistroRequest(email, "Password123!", "Usuario", "La Paz"));
        Assert.Equal(HttpStatusCode.OK, registro.StatusCode);

        if (rolExtra is not null)
        {
            using var scope = factory.Services.CreateScope();
            var userManager = scope.ServiceProvider.GetRequiredService<UserManager<ApplicationUser>>();
            var usuario = await userManager.FindByEmailAsync(email);
            await userManager.AddToRoleAsync(usuario!, rolExtra);
        }

        var login = await cliente.PostAsJsonAsync("/api/auth/login", new LoginRequest(email, "Password123!"));
        Assert.Equal(HttpStatusCode.OK, login.StatusCode);
        return (await login.Content.ReadFromJsonAsync<TokenAccesoResponse>())!.AccessToken;
    }

    [Fact]
    public async Task Subir_con_coincidencia_automatica_aprueba_y_marca_verificado()
    {
        using var cliente = factory.WithWebHostBuilder(b => b.ConfigureServices(s =>
        {
            s.AddSingleton<IVerificadorIdentidadArgos>(_ =>
                new VerificadorArgosEstatico(Result.Exito(new VerificacionFacialResultado(true, 91.2, null))));
        })).CreateClient();

        var email = Email("kyc-ok");
        var token = await RegistrarYLoguearAsync(cliente, email, null);

        using var subir = Autorizada(HttpMethod.Post, "/api/kyc/", token);
        subir.Content = Formulario();
        var resp = await cliente.SendAsync(subir);
        Assert.Equal(HttpStatusCode.NoContent, resp.StatusCode);

        using var estado = Autorizada(HttpMethod.Get, "/api/kyc/estado", token);
        var dto = await (await cliente.SendAsync(estado)).Content.ReadFromJsonAsync<EstadoKycDto>();
        Assert.Equal("Aprobada", dto!.Estado);
    }

    [Fact]
    public async Task Subir_sin_coincidencia_automatica_rechaza()
    {
        using var cliente = factory.WithWebHostBuilder(b => b.ConfigureServices(s =>
        {
            s.AddSingleton<IVerificadorIdentidadArgos>(_ =>
                new VerificadorArgosEstatico(Result.Exito(new VerificacionFacialResultado(false, 21.0, "Rostros no coinciden"))));
        })).CreateClient();

        var email = Email("kyc-fail");
        var token = await RegistrarYLoguearAsync(cliente, email, null);

        using var subir = Autorizada(HttpMethod.Post, "/api/kyc/", token);
        subir.Content = Formulario();
        var resp = await cliente.SendAsync(subir);
        Assert.Equal(HttpStatusCode.NoContent, resp.StatusCode);

        using var estado = Autorizada(HttpMethod.Get, "/api/kyc/estado", token);
        var dto = await (await cliente.SendAsync(estado)).Content.ReadFromJsonAsync<EstadoKycDto>();
        Assert.Equal("Rechazada", dto!.Estado);
    }

    [Fact]
    public async Task Subir_con_argos_caido_devuelve_503()
    {
        using var cliente = factory.WithWebHostBuilder(b => b.ConfigureServices(s =>
        {
            s.AddSingleton<IVerificadorIdentidadArgos>(_ =>
                new VerificadorArgosEstatico(Result.Fallo<VerificacionFacialResultado>(
                    new Error(ErroresKyc.ServicioVerificacionNoDisponible, "No disponible"))));
        })).CreateClient();

        var email = Email("kyc-down");
        var token = await RegistrarYLoguearAsync(cliente, email, null);

        using var subir = Autorizada(HttpMethod.Post, "/api/kyc/", token);
        subir.Content = Formulario();
        var resp = await cliente.SendAsync(subir);
        Assert.Equal(HttpStatusCode.ServiceUnavailable, resp.StatusCode);
    }

    private sealed class VerificadorArgosEstatico(Result<VerificacionFacialResultado> resultado) : IVerificadorIdentidadArgos
    {
        public Task<Result<VerificacionFacialResultado>> VerificarAsync(
            byte[] imagenDocumento, byte[] imagenSelfie, CancellationToken ct) =>
            Task.FromResult(resultado);
    }
}
```

- [ ] **Step 2: Ajustar `KycFlujoTests.cs` si es necesario**

El test `Subir_aprobar_y_ver_verificado_en_perfil_y_token` usa aprobación manual. Con KYC automático, después de subir la solicitud ya estará `Aprobada` solo si el factory usa el adaptador real. En tests de integración existentes, el adaptador real no está configurado, por lo que probablemente devolverá 503. Opciones:

a) Reemplazar `IVerificadorIdentidadArgos` en el factory del test existente para simular aprobación automática, o
b) Convertir el test en un test de "subir sin ARGOS devuelve 503" y mover el flujo aprobado a `KycArgosFlujoTests`.

Decisión recomendada: actualizar `KycFlujoTests` registrando un fake que aprueba automáticamente, y simplificar el test para no llamar a `/api/admin/kyc/{id}/aprobar`.

- [ ] **Step 3: Ejecutar tests de integración**

Run:

```powershell
dotnet test tests/CaseritoApp.IntegrationTests/CaseritoApp.IntegrationTests.csproj --filter "FullyQualifiedName~Kyc"
```

Expected: all green. Requiere Docker.

- [ ] **Step 4: Commit**

```bash
git add tests/CaseritoApp.IntegrationTests/KycArgosFlujoTests.cs

git add tests/CaseritoApp.IntegrationTests/KycFlujoTests.cs

git commit -m "test(kyc-argos): tests de integracion con ARGOS mockeado"
```

---

### Task 12: Verificación global y formato

**Files:**
- All modified files.

- [ ] **Step 1: Build completo**

Run (desde `CaseritoApp/`):

```powershell
dotnet build CaseritoApp.sln
```

Expected: success, 0 warnings.

- [ ] **Step 2: Tests unitarios**

Run:

```powershell
dotnet test tests/CaseritoApp.UnitTests/CaseritoApp.UnitTests.csproj
```

Expected: all green.

- [ ] **Step 3: Tests de integración**

Run:

```powershell
dotnet test tests/CaseritoApp.IntegrationTests/CaseritoApp.IntegrationTests.csproj
```

Expected: all green. Requiere Docker.

- [ ] **Step 4: Formato**

Run:

```powershell
dotnet format CaseritoApp.sln --verify-no-changes
```

Expected: 0 changes needed. Si hay cambios, ejecutar `dotnet format CaseritoApp.sln` y commitear.

- [ ] **Step 5: Frontend checks**

Run (desde `web/`):

```powershell
npm run generate:api
npm run typecheck
npm run lint
npm run test -- --run
npm run build
```

Expected: all green.

- [ ] **Step 6: Commit final**

```bash
git commit -m "chore(kyc-argos): verificacion global y formato" --allow-empty
```

---

## Self-Review

### 1. Spec coverage

| Spec requirement | Task |
|------------------|------|
| Usuario sube foto DNI + selfie desde `KycPage.tsx` | Task 8 (mensajes automáticos), ya existente. |
| Backend valida, guarda blobs cifrados y llama ARGOS | Task 3, Task 4. |
| Si `verified: true` → `Aprobada` + `UserVerified` | Task 3. |
| Si `verified: false` o sin rostro → rechazo con motivo | Task 3, Task 4. |
| Si ARGOS no responde → 503 sin persistir | Task 3, Task 4, Task 6. |
| Registrar `ScoreSimilitud` en `SolicitudKyc` | Task 1, Task 5, Task 7. |
| Aprobación con actor de sistema | Task 1, Task 3. |
| ARGOS solo backend | Task 4 (no expuesto a frontend), Task 8/9 (frontend no llama ARGOS). |
| Anti-PII (no loguear imágenes/base64/embeddings/DNI) | Task 4 logging controlado, Task 1-3 no manejan bytes sensibles. |
| Fail-fast sin `Argos:Url` fuera de dev/testing | Task 4. |
| Tests unitarios e integración | Task 10, Task 11. |
| Frontend refleja verificación automática | Task 8, Task 9. |
| Umbral default de ARGOS (0.68) | Task 4 delega a ARGOS, no configura umbral. |

**Gaps:** ninguno identificado.

### 2. Placeholder scan

- No hay "TBD", "TODO", "implement later" ni "fill in details".
- Cada paso incluye código o comando concreto.
- Los tipos y nombres son consistentes entre tareas (`VerificacionFacialResultado`, `SistemaActor.Id`, `ScoreSimilitud`, etc.).

### 3. Type consistency

- `VerificacionFacialResultado` definido en Task 2 y usado en Task 3, 4, 10, 11 con los mismos nombres de propiedades.
- `SistemaActor.Id` definido en Task 1 y usado en Task 3, 9.
- `ScoreSimilitud` definido en Task 1, mapeado en Task 5, expuesto en Task 7, usado en Task 10.
- Errores `VerificacionFacialFallida` y `ServicioVerificacionNoDisponible` definidos en Task 1 y usados en Task 3, 4, 6, 10, 11.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-28-kyc-argos.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration. REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.

**2. Inline Execution** — Execute tasks in this session using `superpowers:executing-plans`, batch execution with checkpoints for review.

**Which approach?**
